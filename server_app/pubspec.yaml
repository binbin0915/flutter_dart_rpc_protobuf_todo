name: server_app
description: A web server built using the shelf package.
# version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  args: ^1.6.0
  async: ^2.4.2
  grpc: ^2.3.0
  protobuf: ^1.0.1
  rxdart: ^0.24.1

dev_dependencies:
  pedantic: ^1.9.0

